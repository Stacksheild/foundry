{
  "name": "@foundry/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "echo \"no production build yet — see docs/development-plan.md Phase 3\"",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@foundry/agent-core": "workspace:*",
    "@foundry/app-registry": "workspace:*",
    "@foundry/compute-providers": "workspace:*",
    "@foundry/scanner-service": "workspace:*",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^3.0.0"
  }
}
