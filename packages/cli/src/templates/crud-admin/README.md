# my-crud-admin

Scaffolded by `foundry create --template crud-admin`. In-memory CRUD REST API
(`/api/items`) + a table UI. Data resets on every restart — swap the in-memory
array in `src/server.js` for a real database when you're ready.

## Run it

```bash
npm start
```

Then open http://localhost:3000.
